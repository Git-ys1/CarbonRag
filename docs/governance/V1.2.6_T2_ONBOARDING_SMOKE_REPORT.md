# V1.2.6 #2/tbx Onboarding Smoke Report

版本：V1.2.6
日期：2026-05-06
席位：#2 / tbx
GitHub 登录账号：`tbx2835066135`

## 目标

验证 #2 可以从云端主仓库独立完成 fork、clone、upstream 配置、OpenSpec propose、本地前后端验证和 PR 提交流程；同时把 #2 云端复刻环境暴露出的最小主线问题提交给 #1 审查。

## 仓库与分支

- Upstream：`https://github.com/Git-ys1/CarbonRag`
- Fork：`https://github.com/tbx2835066135/CarbonRag`
- 本地分支：`t2/v1.2/onboarding-smoke`
- PR 目标：`Git-ys1/CarbonRag:main`
- OpenSpec change-id：`t2-onboarding-smoke`
- 影响模块：M5 Knowledge / File / RAG；M8 Spec / Governance / Project Docs

## 已执行记录

| 项目 | 结果 |
| --- | --- |
| 删除错误仓库 | 已由用户在 GitHub 网页删除旧的 private 非 fork 仓库 |
| 创建 fork | 通过 GitHub CLI 创建，REST API 确认 `fork=true`、parent/source 为 `Git-ys1/CarbonRag` |
| clone | 已 clone `tbx2835066135/CarbonRag` 到 `E:\CarbonRag` |
| upstream | 已添加 `https://github.com/Git-ys1/CarbonRag.git` |
| 分支 | 已从 `upstream/main` 创建 `t2/v1.2/onboarding-smoke` |
| Git 身份 | 本仓库 `user.name=tbx`，`user.email=2835066135@qq.com` |
| OpenSpec CLI | 已安装并确认 `openspec --version` 为 `1.3.1` |
| 初始 OpenSpec 校验 | `openspec validate --all` 通过 8 个 specs |
| 首次 bootstrap | 前端 typecheck/build 通过；后端 pytest 首次为 95 passed / 11 failed |
| 问题定位 | 共享 private sample bootstrap 少拼 `corpus/`，导致云端 clone 后 private/mixed 检索相关测试失败 |
| 最小修复 | `backend/app/knowledge/service.py` 改为从 `data/private_sample/corpus/**` 解析 manifest filepath |
| 测试隔离 | 会话/SSE 测试改用 fake provider/stream，避免无真实模型 key 的 #2 本地环境误连默认云端模型 |
| 完整后端回归 | `python -m pytest tests` 通过 106 个 tests |
| 本地前后端 smoke | 后端 `healthz` 与前端根页面均返回 200 |

## 本轮变更范围

- 添加本报告作为 #2 入场 smoke 的审查证据。
- 在 `快速上手.md` 增加本报告索引。
- 添加 `openspec/changes/t2-onboarding-smoke/` 产物。
- 修复共享 private sample 路径解析，让主仓库云端 clone 后可以索引 repo 内企业样例。
- 调整测试隔离，使 session/SSE 行为测试不依赖真实模型 key 或外部网络。
- 不修改业务 API、数据库 schema、权限、部署配置或模型/provider 生产行为。

## 验证结果

| 命令或检查 | 结果 |
| --- | --- |
| `openspec validate t2-onboarding-smoke --strict` | 通过 |
| `openspec validate --all` | 通过，9 passed |
| `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/bootstrap.ps1` | 前端通过；首次后端暴露 11 个失败，已按本报告修复并重跑 |
| `cd backend; .\.venv\Scripts\python.exe -m pytest tests` | 通过，106 passed / 4 warnings |
| `cd frontend; npm.cmd run typecheck` | 通过 |
| `cd frontend; npm.cmd run build` | 通过，保留 Vite chunk size warning |
| 后端 `http://127.0.0.1:8000/healthz` | 通过，HTTP 200，`{"status":"ok"}` |
| 前端 `http://127.0.0.1:5173` | 通过，HTTP 200 |
| `git ls-files -o --exclude-standard` | 仅显示本轮报告与 OpenSpec change 产物；忽略文件未进入提交 |
| PR 创建 | 待完成 |

## PR 填写口径

- Version：`V1.2.6`
- Team：`#2 / t2`
- OpenSpec Change ID：`t2-onboarding-smoke`
- Affected specs：`governance`, `knowledge-rag`
- Modules：M5, M8
- Risk：Docs plus minimal M5 bug fix; no API/DB/auth/deploy/provider contract change
- Verification：填写本报告中的实际命令结果
- Approval：等待 #1 final mainline approval
